import styled from 'styled-components';
import { Box } from '@mui/material';

export const BoxSearchTodo = styled(Box)`
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 20px;
`;
